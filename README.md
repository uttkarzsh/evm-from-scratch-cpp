# 💻 EVM from scratch (C++)

A minimal implementation of the **Ethereum Virtual Machine (EVM)** built entirely from scratch in **C++**, compiled with **CMake.**
Inspired by [w1nt3r.eth](https://github.com/w1nt3r-eth/evm-from-scratch).

## ⚙️ Build & Run

```bash
mkdir build && cd build
cmake ..
cmake --build .
./evm
```